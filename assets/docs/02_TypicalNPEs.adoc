= NullPointerException - the common issues
:icons: font

=== 1. Non-determinism
[.small]
[source, java, line, linenums, start=147]
----
inventory.findCoffee().brewingSuggestion().toUpperCase();
----
Stack trace indicates NPE on *line 147* — but which call caused it?

[.small]
----
"Exception in thread "main" java.lang.NullPointerException
     at App.prepareOrder(App.java:147)
     at App.main(App.java:10)
----
~Java~&nbsp;~14~&nbsp;~*Helpful*~&nbsp;~*NullPointerExceptions*~&nbsp;~helped~&nbsp;~alleviate.~

=== 2. Signatures hide `null` returns

Does the call to `findCoffee()` return a null? Wish it read `*findCoffeeOrReturnNull()*`?

=== 3. Hidden future bugs

*Sprint 1*: null check was not needed (only valid values returned).

*Sprint 4*: same check now hides an exception that returns a null - the bug ships silently ☠️‼️ +
~(yes,&nbsp;we&nbsp;have&nbsp;a&nbsp;testing&nbsp;tech.debt)~

'''
[caption=" ", .center, cols="<40%, ^20%, >40%", width=95%, grid=none, frame=none]
|===
| link:01_WhyNullExists.adoc[◀️ 1. Why `null` exists]
| link:Agenda.adoc[Agenda 🔼]
| link:03_Solutions.adoc[3. Common Solutions  ▶️]
|===
