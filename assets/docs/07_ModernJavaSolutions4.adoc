= Modern Java Solutions - Part 4
:icons: font

== Pattern Matching (Java 16+)

`instanceof` on `null` always returns false — no NPE, no guard needed.

== Switch Null Case (Java 21+)

`null` is first-class case — language handles it explicitly.

== Optional Upgrades (Java 9)

`Optional.or()` chains sources, `Optional.stream()` keeps pipeline clean.

